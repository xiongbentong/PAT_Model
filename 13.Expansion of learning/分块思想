对一个有N个元素的有序序列来说，除最后一块外，其余每块中元素个数均为|sqrt(N)|(此处为向下取整，方便程序实现)，于是块数为|sqrt(N)|(此处为向上取整）。
设置一个hash数组table[100001],table[x]表示整数x的当前存在个数。
逻辑上将0~10^5分为317块，每块元素316。
0,1,2...314,315为第0块
316,317,...,630,631为第1块
99856,99857，...，100000为第316块
可以定义一个统计数组block[317],其中block[i]表示第i块中存在的元素个数。
若增加一个元素x，可计算出块号为x/316,然后让block[x/316]加1，table[x]+1
